# Vercel KV Redis Configuration
# Get these values from your Vercel dashboard after creating a KV store
# Navigate to: Storage > KV > Your KV Store > .env.local tab

KV_URL=your_kv_url_here
KV_REST_API_URL=your_rest_api_url_here
KV_REST_API_TOKEN=your_rest_api_token_here
KV_REST_API_READ_ONLY_TOKEN=your_read_only_token_here

# How to set up Vercel KV:
# 1. Go to your Vercel dashboard
# 2. Navigate to Storage tab
# 3. Click "Create Database" > "KV"
# 4. Give it a name (e.g., "rpsls-game-state")
# 5. Copy the environment variables from the .env.local tab
# 6. Paste them into your local .env.local file
# 7. For production, these will be automatically set by Vercel
